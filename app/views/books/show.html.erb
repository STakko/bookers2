<p>show</p><div class="container px-5 px-sm-0">
  <div class="row">
    <div class="col-md-3">newhtmlerbを入れる。</div>
    <div class="col-md-8" offset-md-1">
      <h2>Book detail</h2>
      <table>
        <tbody>
        <% @books.each do |book| %>
          <tr>
            <td>
              <%= link_to user_path(book.user.id) do %>
                <%= attachment_image_tag book.user, :profile_image, :fill, 40, 55, fallback: "no_image.jpg %>
              <% end %>
            </td>
            <td>
              <%= link_to book.title, book_path(book.id) %>
            </td>
            <td>
              <%= book.body %>
            </td>
          </tr>
        <% end %>
        </tbody>
      </table>
    </div>
  </div>
</div>